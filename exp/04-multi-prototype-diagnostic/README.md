# Multi-prototype diagnostic

Start with retrieval-log analysis before implementing slots. Construct matched prompts with
one, two, and four persistent entities. Measure top-1 margin, entropy, wrong-chunk selection,
and small-attribute recovery. Mean-pooling dilution is supported only if confidence and
recovery degrade monotonically with entity count while visual quality remains comparable.

